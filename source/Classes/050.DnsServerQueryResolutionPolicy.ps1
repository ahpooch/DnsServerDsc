<#
    .SYNOPSIS
        The DnsServerQueryResolutionPolicy DSC resource manages Query Resolution Policies that control how incoming resolution queries are handled by a DNS server.

    .DESCRIPTION
        The DnsServerQueryResolutionPolicy DSC resource manages Query Resolution Policies that control how incoming resolution queries are handled by a DNS server.

    #.PARAMETER ZoneScope
    #    Specifies the name of a zone scope. (Key Parameter)

    .NOTES
        DNS Policies overview
        https://learn.microsoft.com/en-us/windows-server/networking/dns/deploy/dns-policies-overview
#>
